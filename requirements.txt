# 온프레미스 법조문 RAG — 의존성
# 외부 임베딩/LLM API 미사용. 모든 모델은 로컬에서 구동.
# 버전은 모두 고정(==). venv(Python 3.12)에서 실제 설치·검증된 조합.

# 설정 로딩
PyYAML==6.0.3

# 벡터 DB (로컬·소규모, 파일 임베디드 모드 — 서버 불필요)
qdrant-client==1.18.0

# 임베딩 / 리랭커 (cross-encoder)
sentence-transformers==5.6.0
torch==2.12.0

# Sparse 검색 (BM25)
rank-bm25==0.2.2

# 한국어 토크나이저 (BM25용) — 형태소 분석
kiwipiepy==0.23.2

# 평가
ragas==0.4.3
# ragas 0.4.3이 langchain-community의 vertexai 연동 모듈을 무조건 import하는데,
# langchain-community>=0.4 부터는 해당 모듈이 빠져 ImportError가 발생함.
# 0.3.x로 고정해야 ragas import가 정상 동작함 (langchain/langchain-core 1.x와도 호환 확인됨).
langchain-community==0.3.31

# 유틸
numpy==2.4.6
pandas==3.0.3
tqdm==4.68.3
